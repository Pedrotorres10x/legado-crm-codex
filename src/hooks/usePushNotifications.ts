/**
 * Web-only push notifications stub.
 * Push notifications require native platform — this is a no-op on web.
 */
export function usePushNotifications(_userId: string | undefined) {
  // No-op on web
}
